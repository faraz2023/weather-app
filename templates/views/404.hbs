<!DOCTYPE html>

<html>

{{>document_header}}

<body>
    <div class="main-content">
        <div class='container'>
            <div class='row'>
                <div class='col-md-12'>
                    {{>header}}
                    <h3>{{message}}</h3>
                </div>
            </div>
        </div>
    </div>

    {{>footer}}
</body>

</html>